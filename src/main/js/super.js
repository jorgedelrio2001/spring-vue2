//replace the information here with the name of your json file, feel free to change
// the name of your file and set the path in "ticket" to match :)
//I just set the file in js
//do not save your json file to git, please. only for testing purposes
/*
import * as path from "path";
const firePath=process.env.cdfb;//replace this with the absolute address of your config file
function baseDir (filename){
    return path.join(firePath,filename);
}
export default{
    ticket:baseDir(firePath,"adiam.json")
}*/